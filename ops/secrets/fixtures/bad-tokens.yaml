# bad-tokens.yaml — KNOWN-BAD fixture for schema validator testing
#
# This file intentionally violates mcp-tokens.schema.yaml in multiple ways:
#   1. Missing required field "token_hash" on the first entry
#   2. "issued" date in wrong format (YYYY/MM/DD instead of YYYY-MM-DD)
#   3. "identity" using an invalid pattern (spaces not allowed)
#
# The validator MUST reject this file with schema violation errors.
# Used by: pnpm validate:mcp-config --tokens ops/secrets/fixtures/bad-tokens.yaml

tokens:
  - identity: "invalid identity with spaces"
    # token_hash is intentionally missing (required field)
    issued: "2026/06/09"
    notes: "This entry is intentionally broken"
