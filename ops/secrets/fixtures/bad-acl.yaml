# bad-acl.yaml — KNOWN-BAD fixture for schema validator testing
#
# This file intentionally violates mcp-acl.schema.yaml in multiple ways:
#   1. "identities" key is missing entirely (top level is wrong shape)
#   2. Demonstrates the validator rejects files with wrong structure
#
# The validator MUST reject this file with schema violation errors.
# Used by: pnpm validate:mcp-config --acl ops/secrets/fixtures/bad-acl.yaml

# Wrong top-level key — should be "identities:"
permissions:
  platform:hermes-gateway:
    allow:
      - cashflow.add
